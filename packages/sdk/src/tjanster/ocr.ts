/**
 * Tjänsten `ocr` för appar (`/_api/ocr`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/ocr.md och visas bara när tjänsten är påslagen.
 */
export {};
