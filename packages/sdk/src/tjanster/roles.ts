/**
 * Tjänsten `roles` för appar (`/_api/roles`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/roles.md och visas bara när tjänsten är påslagen.
 */
export {};
