/**
 * Tjänsten `notify` för appar (`/_api/notify`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/notify.md och visas bara när tjänsten är påslagen.
 */
export {};
