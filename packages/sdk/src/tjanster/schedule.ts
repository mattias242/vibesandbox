/**
 * Tjänsten `schedule` för appar (`/_api/schedule`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/schedule.md och visas bara när tjänsten är påslagen.
 */
export {};
