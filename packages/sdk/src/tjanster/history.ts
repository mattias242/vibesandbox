/**
 * Tjänsten `history` för appar (`/_api/history`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/history.md och visas bara när tjänsten är påslagen.
 */
export {};
