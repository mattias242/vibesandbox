/**
 * Tjänsten `transcribe` för appar (`/_api/transcribe`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/transcribe.md och visas bara när tjänsten är påslagen.
 */
export {};
