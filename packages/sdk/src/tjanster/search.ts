/**
 * Tjänsten `search` för appar (`/_api/search`). Under uppbyggnad — ännu inga funktioner.
 * Anropa tjänsten bara genom `callService` i ./anrop.ts. Dokumentationen för byggagenten
 * skrivs i packages/sdk/tjanster/search.md och visas bara när tjänsten är påslagen.
 */
export {};
